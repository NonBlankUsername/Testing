#include "../Globals.hpp"

namespace Globals {
    namespace ESP {
        ImColor BoxColor = ImColor(255, 0, 0);  // Example default color for the box
        ImColor NameColor = ImColor(255, 255, 255);  // Example default color for name
        ImColor HealthTextColor = ImColor(255, 255, 255);  // Default color for health text
        ImColor FilledBoxColor = ImColor(0, 0, 0, 150);  // Default color for the filled box

    }
}
